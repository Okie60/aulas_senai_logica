using static System.Console;
string name;
WriteLine("digite seu nome e blablabla:");
name = ReadLine();
if (name == "rogerin")
{
    WriteLine("Bem vindo, senhor rogerin!");
}
else
{
    WriteLine("seu nome é " + name);
}